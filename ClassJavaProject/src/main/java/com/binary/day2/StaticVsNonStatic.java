package com.binary.day2;

public class StaticVsNonStatic {

    public static void main(String[] args) {


        // Static :
        // keyword : static
        // Static members (attributes and method) its always public.
        // Static members (attributes and method ) are always associated with class itself,
        //                                           not with individual objects.
        // Static members (attributes and method ) can only be accessed by Class reference.
        // Static members (attributes and method ) can only be changed or called using static members.


        // Non - static :

        // if we don't see a static keyword before attribute or method, then its non-static.


        // create a Class => create 4 private Attributes.
        // create a methods to take input to change Attribute values (setters)
        // create a methods to access attribute values. (getters)
        // in Each class add least one Static Attribute and one Static Method to modify the Static Attribute.
        // 3 objects of class.

        // ----------------------------------------------------------------------------------------------

        //        Each Class create 3 Objects.
        // Laptop       Attributes -> OS, price, rating, processor, ram , screenSize
//                       Methods ->   switchOn(), switchOff(), openApp(), closeApp().

//         House         Attributes -> houseType, noOfBedRooms, noOfBathrooms, location, price, isItInCity
//                       Methods    -> enter(), sold(), rent().


//         Phone         Attributes -> screenSize, batterLife, memorySize, color, price, model, ram, brand, camera.
//                       Methods    -> calling(), texting(), playMusic(), playVideo(), chargePhone(), turnOff(), turnOn().
//                                        volumeUp(), volumeDown().




    }


}
